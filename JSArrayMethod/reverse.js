// the reverse() method reverses the order of the elements in an array.
// This method overwrites the original array.

// array.reverse();

let numbers = [1, 2, 3,11, 7, 6, 5];
let strng = ["A", "B", "C", "D"];

console.log(numbers.reverse()); // [5, 6, 7, 11 3, 2, 1]
console.log(strng.reverse()); // [ 'D', 'C', 'B', 'A' ]