// The slice() slices out a piece from an array. it creates a new array.

// array.slice(start, end);

let number = [1, 2, 4, 5, 6, 76, 4];

let num2 = number.slice(1, 3);
console.log(num2); // [ 2, 4 ]